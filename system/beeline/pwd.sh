# The goal is to get the gist of where I am, not the full path
# If I need the full path, I'm just going to run pwd anyway

# Requirements:
# - Must display immediate "special" parent and name of current
#   directory, everything else can be truncated (...)
#   - Git (   dropbear  production )